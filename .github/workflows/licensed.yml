name: Licensed

on:
  push:
    branches:
      - sample-787
  pull_request:
    branches:
      - main

jobs:
  call-licensed:
    name: Licensed
    uses: actions/reusable-workflows/.github/workflows/licensed.yml@main
