name: Licensed

on:
  push:
    branches:
      - samplev4
  pull_request:
    branches:
      - main

jobs:
  call-licensed:
    name: Licensed
    uses: actions/reusable-workflows/.github/workflows/licensed.yml@main
